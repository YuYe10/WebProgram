\documentclass[12pt,a4paper]{article}

\usepackage[UTF8]{ctex}
\usepackage[margin=2cm,top=2.2cm,bottom=2.2cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{tikz}
\usepackage{tabularx}
\usepackage{float}
\usepackage{caption}

\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\floatplacement{figure}{htbp}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!70!black,
  urlcolor=blue!70!black,
  citecolor=blue!70!black
}

\lstset{
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  frame=single,
  backgroundcolor=\color{gray!5},
  keywordstyle=\color{blue!70!black},
  commentstyle=\color{green!50!black},
  stringstyle=\color{red!60!black},
  columns=fullflexible,
  keepspaces=true,
}

\title{\textbf{Noteworthy}  轻量化在线笔记软件\\项目说明文档}
\author{姓名：俞烨 \quad 学号：102301116\\选题方向：在线笔记软件}
\date{\today}

\begin{document}
\maketitle
\thispagestyle{empty}
\tableofcontents
\pagebreak

\section{项目概述}

Noteworthy 是面向个人知识管理的轻量化在线笔记平台，采用前后端分离架构，提供专业级富文本编辑、JWT 双令牌认证、全文搜索等核心功能。

\subsection{核心目标与运行环境}

Noteworthy 的核心目标包括三个方面：提供专业级富文本编辑体验、实现安全可靠的用户认证机制、支持灵活的笔记组织与检索。系统运行需要 Docker 20.10+、Python 3.10+、Node.js 18+ 和 PostgreSQL 16+ 等环境支持。

\subsection{快速启动指南}

启动系统需要三个步骤：首先启动数据库容器，然后启动后端服务，最后启动前端服务。具体命令如下：

\begin{lstlisting}[language=bash]
# 启动数据库
docker compose up -d db

# 启动后端
cd backend && cp .env.example .env
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt && alembic upgrade head
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 启动前端
cd frontend && npm install && npm run dev
\end{lstlisting}

启动后，可访问 \url{http://localhost:8000/docs} 查看 API 文档，访问 \url{http://localhost:5173} 使用应用。

\section{技术架构}

\begin{table}[h]
\centering
\small
\begin{tabularx}{\textwidth}{llX}
\toprule
层次 & 技术 & 选型理由 \\
\midrule
前端框架 & Vue 3 + TypeScript & 采用 Composition API，支持全量类型安全 \\
构建工具 & Vite 6 & 原生 ESM 热更新，冷启动时间低于 300 毫秒 \\
状态管理 & Pinia & Vue 3 官方推荐，类型推导能力完善 \\
富文本 & Tiptap & 基于 ProseMirror 内核，支持表格和代码高亮 \\
后端框架 & FastAPI & 原生支持异步编程，自动生成 API 文档 \\
ORM & SQLAlchemy 2.0 & 声明式映射配合异步引擎，类型提示完备 \\
数据库 & PostgreSQL 16 & 支持 JSONB 存储和 pg\_trgm 模糊搜索 \\
认证 & JWT 双令牌 & 无状态鉴权，支持无感续期机制 \\
\bottomrule
\end{tabularx}
\caption{技术栈选型}
\end{table}

后端采用四层架构：Router → Service → Repository → Model。主界面如图 \ref{fig:main} 所示：

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/main.png}
\caption{Noteworthy 主界面}
\label{fig:main}
\end{figure}

\section{核心功能实现}

用户认证模块采用 JWT 双令牌机制，Access Token 有效期为 30 分钟，用于接口鉴权；Refresh Token 有效期为 7 天，用于无感续期。前端通过 Axios 拦截器实现请求队列模式，避免并发刷新竞态问题。登录界面如图 \ref{fig:login} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/登录界面.png}
\caption{用户登录界面}
\label{fig:login}
\end{figure}

富文本编辑模块基于 Tiptap 构建，集成了 15 种以上扩展功能，支持标题、列表、表格、代码高亮等操作。编辑界面如图 \ref{fig:editor} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/note-edit.png}
\caption{笔记编辑界面}
\label{fig:editor}
\end{figure}

保存策略包含三重机制：内容变更后 3 秒自动保存、支持 Ctrl+S 快捷键手动保存、组件卸载时自动执行兜底保存。全文搜索模块基于 PostgreSQL 的 ILIKE 模糊匹配实现，可以按笔记本进行过滤，搜索界面如图 \ref{fig:search} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/search.png}
\caption{全文搜索界面}
\label{fig:search}
\end{figure}

\section{技术难点与解决方案}

开发中遇到的第一个挑战是 JWT 并发刷新竞态问题。当多个请求同时收到 401 状态码时，如果各自独立刷新令牌，后到的刷新请求会使先前的 Refresh Token 失效，导致其他请求失败。解决方案是在 Axios 拦截器中实现请求队列模式：

\begin{lstlisting}
let isRefreshing = false
let failedQueue = []

const processQueue = (error, token) => {
  failedQueue.forEach(promise => {
    if (error) {
      promise.reject(error)
    } else {
      promise.resolve(token)
    }
  })
  failedQueue = []
}
\end{lstlisting}

第二个挑战是图片存储泄漏问题。用户删除笔记或编辑移除图片后，文件系统可能残留无人引用的图片文件，长期运行会耗尽磁盘空间。解决方案采用双层清理策略：即时清理在笔记更新时对比新旧图片引用，立即删除不再被引用的文件；定时扫描通过后台任务每小时执行全量扫描，删除无主文件与过期归档笔记。

\section{项目结构与笔记本管理}

后端采用 Router→Service→Repository→Model 四层架构，前端按功能域组织。核心目录结构如下：

\begin{lstlisting}[language={}]
WebProgram/
+-- backend/app/            # FastAPI 后端
|   +-- main.py             # 应用入口
|   +-- api/v1/             # API 路由
|   +-- core/services/      # 核心服务
|   +-- models/schemas/     # 数据模型
+-- frontend/src/           # Vue 3 前端
|   +-- api/stores/         # API 与状态管理
|   +-- components/views/   # 组件与视图
+-- docs/                   # 项目文档
\end{lstlisting}

笔记本管理模块支持创建、编辑、删除笔记本，用户可以将笔记组织到不同的笔记本中。创建笔记本界面如图 \ref{fig:create-notebook} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/create-Notebook.png}
\caption{创建笔记本界面}
\label{fig:create-notebook}
\end{figure}

笔记本内的笔记列表界面如图 \ref{fig:notebook-notes} 所示，支持查看和管理该笔记本下的所有笔记。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/Notebook内Notes界面.png}
\caption{笔记本内笔记列表界面}
\label{fig:notebook-notes}
\end{figure}

\section{API 接口概览与标签管理}

系统共暴露 16 个 API 端点，包括用户认证（登录/刷新）、笔记本管理（CRUD）、笔记操作（CRUD/置顶/归档）、标签管理、全文搜索和图片上传等接口。完整 API 文档可访问 \url{http://localhost:8000/docs} 查看。

标签管理模块支持创建、编辑、删除标签，并可将标签与笔记关联，帮助用户更好地组织和分类笔记内容。

\section{笔记归档与学习总结}

笔记归档功能允许用户将不再需要的笔记归档，归档的笔记会在 7 天后自动删除。归档界面如图 \ref{fig:archive} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/归档Note界面.png}
\caption{笔记归档界面}
\label{fig:archive}
\end{figure}

\section{学习总结与评估}

通过本项目的开发，我深入掌握了全栈开发流程，涵盖数据库设计、API 开发到前端交互的完整链路。同时学习了 JWT 认证体系与并发刷新队列的实现方法、ProseMirror 和 Tiptap 富文本编辑技术、SQLAlchemy 2.0 异步编程模式，以及泛型 Repository 设计模式。

项目在多个方面表现良好。功能完整性方面，全部 9 个核心模块均已实现；代码质量方面，结构清晰，采用 TypeScript 实现全量类型覆盖；安全机制方面，采用 BCrypt 密码哈希、JWT 双令牌认证和用户级数据隔离。

未来可以考虑引入 WebSocket 实现实时通知功能，支持笔记导出为 Markdown 和 PDF 格式，以及实现协作编辑功能。

\section{原创性声明与引用}

本项目为个人独立完成的设计与开发作品，所有核心代码均为原创，未抄袭或复制任何现有项目的代码。

项目使用的主要开源依赖包括 Vue 3、FastAPI、Tiptap、SQLAlchemy 和 UnoCSS，相关文档可分别访问其官方网站查看。

\end{document}
